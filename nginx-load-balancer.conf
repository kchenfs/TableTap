events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    sendfile on;
    keepalive_timeout 65;
    resolver 127.0.0.11 valid=30s;
    
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                   '$status $body_bytes_sent "$http_referer" '
                   '"$http_user_agent" "$http_x_forwarded_for"';
    
    access_log /var/log/nginx/access.log main;
    error_log /var/log/nginx/error.log warn;
    
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;

    # ==================================
    # == TAKE-OUT DOMAIN CONFIGURATION ==
    # ==================================
    server {
        listen 80;
        server_name take-out.momotarosushi.ca;
        location /.well-known/acme-challenge/ { root /var/www/certbot; }
        location / { return 301 https://$host$request_uri; }
    }
    
    server {
        listen 443 ssl;
        http2 on;
        server_name take-out.momotarosushi.ca;
        ssl_certificate /etc/letsencrypt/live/take-out.momotarosushi.ca/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/take-out.momotarosushi.ca/privkey.pem;
        
        # Set the root for all static files
        root /usr/share/nginx/html;

        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;

        # === CHATBOT ASSETS - HIGHEST PRIORITY ===
        # Serve ALL chatbot assets directly from the mounted volume
        location /chatbot-assets/ {
            alias /usr/share/nginx/html/chatbot-assets/;
            try_files $uri =404;
            
            # Add proper headers for all file types
            add_header Access-Control-Allow-Origin "*" always;
            add_header Access-Control-Allow-Methods "GET, OPTIONS" always;
            add_header Cache-Control "public, max-age=3600" always;
            
            # Remove the X-Frame-Options for HTML files (chatbot needs to load in iframe)
            location ~ \.html$ {
                add_header X-Frame-Options "" always;
            }
        }

        # Rewrite rules for files requested from root (legacy support)
        location = /lex-web-ui-loader.css {
            return 301 /chatbot-assets/lex-web-ui-loader.css;
        }

        location = /lex-web-ui-loader-config.json {
            return 301 /chatbot-assets/lex-web-ui-loader-config.json;
        }
        
        location = /material_icons.css {
            return 301 /chatbot-assets/material_icons.css;
        }

        # Catch other known chatbot dependencies requested from root
        location ~ ^/(3\..+\.(js|css)|4\..+\.js|aws-sdk.+\.js|lex-web-ui.*\.(js|css)|wav-worker.+\.js|chatbot-init-debug\.js)$ {
            return 301 /chatbot-assets/$1;
        }

        location ~ ^/(lex-web-ui-.+\.(js|css|map))$ {
            return 301 /chatbot-assets/$1;
        }
        
        # Main application proxy
        location / {
            proxy_pass http://takeout:80;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    # ========================================
    # == DINE-IN DOMAIN (Corrected Routing) ==
    # ========================================
    server {
        listen 80;
        server_name dine-in.momotarosushi.ca;
        location /.well-known/acme-challenge/ { root /var/www/certbot; }
        location / { return 301 https://$host$request_uri; }
    }

    server {
        listen 443 ssl;
        http2 on;
        server_name dine-in.momotarosushi.ca;
        ssl_certificate /etc/letsencrypt/live/dine-in.momotarosushi.ca/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/dine-in.momotarosushi.ca/privkey.pem;
        
        # Set the root for all static files
        root /usr/share/nginx/html;

        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        
        # === CHATBOT ASSETS - HIGHEST PRIORITY ===
        location /chatbot-assets/ {
            alias /usr/share/nginx/html/chatbot-assets/;
            try_files $uri =404;
            
            add_header Access-Control-Allow-Origin "*" always;
            add_header Access-Control-Allow-Methods "GET, OPTIONS" always;
            add_header Cache-Control "public, max-age=3600" always;
            
            location ~ \.html$ {
                add_header X-Frame-Options "" always;
            }
        }

        # Rewrite rules for files requested from root
        location = /lex-web-ui-loader.css {
            return 301 /chatbot-assets/lex-web-ui-loader.css;
        }

        location = /lex-web-ui-loader-config.json {
            return 301 /chatbot-assets/lex-web-ui-loader-config.json;
        }
        
        location = /material_icons.css {
            return 301 /chatbot-assets/material_icons.css;
        }

        location ~ ^/(3\..+\.(js|css)|4\..+\.js|aws-sdk.+\.js|lex-web-ui.*\.(js|css)|wav-worker.+\.js|chatbot-init-debug\.js)$ {
            return 301 /chatbot-assets/$1;
        }
        
        location ~ ^/(lex-web-ui-.+\.(js|css|map))$ {
            return 301 /chatbot-assets/$1;
        }

        # --- Table routing ---
        location /table-1/ {
            proxy_pass http://table-1:80/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        location /table-2/ {
            proxy_pass http://table-2:80/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        location /table-3/ {
            proxy_pass http://table-3:80/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        location /table-4/ {
            proxy_pass http://table-4:80/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        location /table-5/ {
            proxy_pass http://table-5:80/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        location /table-6/ {
            proxy_pass http://table-6:80/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        location /table-7/ {
            proxy_pass http://table-7:80/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        location /table-8/ {
            proxy_pass http://table-8:80/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        location /table-9/ {
            proxy_pass http://table-9:80/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        location /table-10/ {
            proxy_pass http://table-10:80/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        location /table-11/ {
            proxy_pass http://table-11:80/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        
        # The default route for the main domain now correctly redirects to table-1
        location = / {
            return 301 https://$host/table-1/;
        }
    }
}