apiVersion: v1
kind: Service
metadata:
  name: tabletap-service
  namespace: default
  labels:
    app: tabletap
spec:
  type: ClusterIP
  selector:
    app: tabletap
  ports:
    - name: web
      port: 80
      targetPort: 80
    - name: metrics
      port: 9113
      targetPort: 9113